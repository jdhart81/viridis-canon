# Summary of changes for run c7a0a240-7582-49c7-a500-f44ef030f0c9
Discharged every `sorry` in `EcopoieticOrdering.lean`. All 11 named theorems compile with no `sorry`/`admit`; axiom audit for each is exactly `{propext, Classical.choice, Quot.sound}`; and each conclusion is a genuine (non-vacuous) statement — witnessed concretely by `esot_ordering_nonvacuous` where the wall strictly binds.

What was proved (plus supporting lemmas): the R1 antitonicity `residual_antitone_in_established`; extensivity/monotonicity of the one-step operator (`reachStep_extensive`, `reachStep_mono_in_established`, `reachStep_mono_in_budget`); the finite-iterate closure facts (`reachClosure_extensive`, `reach_closure_is_fixpoint`, `reach_closure_monotone_in_budget`); the threshold up-set form `reachability_wall_is_up_set`; and the sharp minimax=critical-budget results `reachability_critical_budget_eq_minimax_bottleneck` (as `IsLeast`) and `reachability_iff_budget_geq_minimax_bottleneck`. Core machinery includes a generic finite-lattice fixpoint lemma (an extensive operator stabilizes within `card+1` steps), the two build-order⇄reachable-closure bridges (`order_mem_fixpoint`, `build_order_exists`), and the finite-infimum characterization of `Bstar` (`bottleneck_finite/nonempty`, `Bstar_mem`, `Bstar_le_of_perm`).

Two flagged deviations were mathematically necessary — each of the two wall theorems is FALSE as originally deferred, verified by explicit counterexamples:

(A) `obAux` base case. The stub `obAux _ [] = 0` folds a spurious `max(·,0)` into the END of every build order, forcing `Bstar ≥ 0` always; e.g. with `Psi0 ≡ -5`, `f ≡ 0`, `seed = ∅` the biosphere is reachable at budget `-3` yet the stub gives `Bstar = 0 > -3`. I strengthened this auxiliary definition to the faithful "largest residual actually crossed" by giving the singleton build `[k]` its own base case (`obAux S [k] = residual … k S`); the `[] ↦ 0` clause is retained only for totality and is never used by a genuine nonempty order.

(B) Nonempty side condition. When `seed = univ` the whole biosphere is already established, so `reachable` holds at EVERY (including arbitrarily negative) budget: the reachable-budget set is all of ℝ, which has no least element and is not `[B⋆,∞)` for any real `B⋆`. Thus `IsLeast … B⋆` and `reachable ↔ B⋆ ≤ θ` are unavoidably false in that single degenerate case for any real `B⋆`. I added the minimal faithful hypothesis `(univ \ seed).Nonempty` (equivalently `seed ≠ univ`) to those two theorems, under which both hold sharply and non-vacuously. Both deviations are documented in the file header (FORGE NOTES A/B) and at the theorems. All remaining named statements and conclusions are preserved verbatim.

One benign linter note remains: `hf` is unused in `reach_closure_is_fixpoint` (the fixpoint needs only extensivity); `hf` is kept because it is part of the stated theorem, with a docstring note explaining this.